/**
 * Kind of interactive prompt used to collect a template input.
 *
 * - `text`: free-form string entry.
 * - `select`: single choice among a predefined list.
 */
export type TemplateInputKind = 'text' | 'select';
