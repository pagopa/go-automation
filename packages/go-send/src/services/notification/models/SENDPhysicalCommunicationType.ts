/**
 * Physical communication type for analog notifications
 */
export enum SENDPhysicalCommunicationType {
  /** Registered Letter with Return Receipt */
  AR_REGISTERED_LETTER = 'AR_REGISTERED_LETTER',
  /** Registered Letter (Simple) */
  REGISTERED_LETTER_890 = 'REGISTERED_LETTER_890',
}
