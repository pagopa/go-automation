import type { lambda } from '../framework.js';

/** No internal downstream log-routing rule is documented for this alarm. */
export const DOWNSTREAM_ERROR_PATTERNS: ReadonlyArray<lambda.DownstreamErrorPattern> = [];
