/**
 * Visual variants supported by `GOTableFormatter`:
 *
 * - `full`: top/middle/bottom separator lines + side borders (default)
 * - `border-less`: only header separator + vertical separators between cells
 * - `compact`: no borders, single space between cells
 */
export type BorderStyle = 'full' | 'border-less' | 'compact';
