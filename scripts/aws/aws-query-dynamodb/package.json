{
  "name": "aws-query-dynamodb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Queries DynamoDB table - Retrieves data items from a DynamoDB table via query.",
  "main": "dist/index.js",
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo node_modules",
    "dev": "tsx src/index.ts",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "start": "pnpm build && node dist/index.js",
    "watch": "tsc --watch"
  },
  "keywords": [
    "aws",
    "dynamodb"
  ],
  "author": "Team GO - Gestione Operativa",
  "license": "ISC",
  "dependencies": {
    "@go-automation/go-common": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:"
  }
}
